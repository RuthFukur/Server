<%- include('upper.ejs')%>
    <ol>
        <% for (let i=0; i < products.length; i++) { %>
            <li>Name: <%= products[i].name %>,
                    Price: <%= products[i].price %>
                        Quantity: <%= products[i].quantity %>
                            <% } %>
            </li>
    </ol>

    <%- include('lower.ejs')%>